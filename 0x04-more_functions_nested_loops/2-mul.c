#include <stdio.h>
#include "main.h"

/**
 * mul - Entry point of the multiplication function
 * @a: this take integer values for check
 * @b: this take integer values for check
 * Description: the fumction take two args and multiplly them to return 0
 * Return: Always return 0
 */

int mul(int a, int b)
{
	int res;

	res = a * b;

	return (res);
}
